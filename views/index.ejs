<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="css/style.css">
    <title>youtube converter</title>
</head>
<body>
    <div class="box-form"> 
        <form action="converter-mp3" method="POST" id="form">
            <input type="text" name="videoURL" placeholder="insert URL">
            <button id="buttonconvert">CONVERT</button>
        </form>
    </div>  
    <div class="container-bottom">
        <% if(typeof success != "undefined" && success){  %>
            <div class="succes">
                <p><%= song_title%></p>
                <a href="<%= song_link %>"><button id="down-btn">DOWNLOAD</button></a>
                <iframe width="560" height="315" src="https://www.youtube.com/embed/<%= locals?.idVideo %>" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen></iframe>
            </div>
        <% } else if(typeof success != "undefined" && !success){  %>
            <div class="err">
                <p><%= message %></p>
            </div>
        <% }else if(typeof spotifySucces != "undefines" && spotifySucces){  %>
            <div class="succes">
                <p><%= song_title %></p>
                <a href="<%= song_link %>"><button id="down-btn" style="color: rgb(0, 189, 0);">DOWNLOAD</button></a>
            </div> 
            <% } %>
    </div>

</body>
</html>